export default function ApplicationLogo({ classNames }) {
    return (
        <img
            className={`${classNames} w-16 h-auto`}
            src="/logonobgsm.webp"
            alt=""
        />
    );
}
